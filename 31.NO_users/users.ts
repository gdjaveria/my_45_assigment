let userNames: string[] = ['Admin','Ariba','Tayyaba','uzma'];

if(userNames.length === 0){
    console.log('we need to find some users..');

} else {
    userNames = [];
    console.log('All users name have been removed.' + userNames.length);

}
